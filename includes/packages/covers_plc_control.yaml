covers_plc_control:

  #######################################################################
  # ⚙️ УНИВЕРСАЛЬНЫЙ СКРИПТ ИМПУЛЬСА НА ВХОД ПЛК
  #######################################################################
  script:
    plc_pulse_send:
      alias: "PLC — отправить импульс"
      description: "Подаёт импульс на указанный вход PLC через сущность light.*"
      mode: parallel
      fields:
        target_entity:
          description: "Сущность light, на которую подаётся импульс"
          example: "light.zal_shtor_open"
        duration:
          description: "Длительность импульса (в секундах)"
          example: 0.2
      sequence:
        - service: light.turn_on
          target:
            entity_id: "{{ target_entity }}"
        - delay:
            seconds: "{{ duration | float(0.2) }}"
        - service: light.turn_off
          target:
            entity_id: "{{ target_entity }}"

  #######################################################################
  # 🪟 COVER (шторы) через шаблон — управление через plc_pulse_send
  #######################################################################
  cover:

    - platform: template
      covers:

        ###############################################################
        # ЗАЛ
        ###############################################################
        zal_cover:
          friendly_name: "Зал — Шторы"
          device_class: curtain
          icon_template: mdi:curtains
          open_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.zal_shtor_open
              duration: 0.2
          close_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.zal_shtor_close
              duration: 0.2

        ###############################################################
        # КУХНЯ
        ###############################################################
        kuhnya_cover:
          friendly_name: "Кухня — Шторы"
          device_class: curtain
          icon_template: mdi:curtains
          open_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.kuhnya_shtor_open
              duration: 0.2
          close_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.kuhnya_shtor_close
              duration: 0.2

        ###############################################################
        # ДЕТСКАЯ
        ###############################################################
        detskaya_cover:
          friendly_name: "Детская — Шторы"
          device_class: curtain
          icon_template: mdi:curtains
          open_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.detskaya_shtor_open
              duration: 0.2
          close_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.detskaya_shtor_close
              duration: 0.2

        ###############################################################
        # СПАЛЬНЯ
        ###############################################################
        spalnya_cover:
          friendly_name: "Спальня — Шторы"
          device_class: curtain
          icon_template: mdi:curtains
          open_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.spal_shtor_open
              duration: 0.2
          close_cover:
            service: script.plc_pulse_send
            data:
              target_entity: light.spal_shtor_close
              duration: 0.2
